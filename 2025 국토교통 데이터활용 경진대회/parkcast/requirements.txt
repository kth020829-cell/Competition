ultralytics>=8.1.0
pycocotools>=2.0.7
opencv-python-headless>=4.8.0
matplotlib>=3.7.0
seaborn>=0.13.0
pandas>=2.0.0
numpy>=1.24.0
tqdm>=4.66.0
PyYAML>=6.0
Pillow>=10.0.0
gradio>=4.19.0

# Cross-lot 평가 (parkcast/domain.py) — 임베딩 클러스터링
scikit-learn>=1.3.0
umap-learn>=0.5.5

# CLIP 기반 VLM 쿼리 탭 (parkcast/vlm.py)
transformers>=4.36.0

# HuggingFace Spaces 배포 (app/hf_space)
huggingface_hub>=0.20.0

# Python 3.13에서 stdlib audioop이 제거됨(PEP 594) — gradio가 오디오 컴포넌트 지원용으로
# pydub → audioop을 무조건 import하다가 죽는 걸 막음(오디오 기능은 안 씀에도 필요).
audioop-lts; python_version >= "3.13"
